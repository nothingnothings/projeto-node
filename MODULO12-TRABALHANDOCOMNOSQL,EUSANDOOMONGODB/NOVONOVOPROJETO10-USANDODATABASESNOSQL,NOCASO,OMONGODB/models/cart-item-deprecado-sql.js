// const Sequelize = require('sequelize');

// const { Sequelize } = require('sequelize');

// const sequelize = require('../util/database');

// const CartItem = sequelize.define('cartItem', { ////cuidado com o primeiro parâmetro; sempre escreva NOMES JUNTOS, E NUNCA NOMES compostots, tipo 'assim-assim' (nunca faça isso, pq buga o ACESSO as propriedades/fields da JOIN TABLE/through table/intermediate table, mais tarde.... -> isso é visto lá naquela escrita de 'product.cartItem.quantity', lá na VIEW de 'cart.ejs'...)
//     id: {
//         type: Sequelize.INTEGER,
//         autoIncrement: true,
//         allowNull: false,
//         primaryKey: true,
//     },
//     quantity: Sequelize.INTEGER

// });

// module.exports = CartItem;
